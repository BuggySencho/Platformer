using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class characterSpwaning : MonoBehaviour
{
    public GameObject[] Characters;
    public Transform PlayerSpawnPoint;
    public GameObject scriptManager;
    public characterSlelction characterSelect;
    // Start is called before the first frame update
    void Start()
    {
        characterSelect = scriptManager.GetComponent<characterSlelction>();
        Instantiate(characterSelect[characterSelect.PlayerNumber], PlayerSpawnPoint.position, PlayerSpawnPoint.rotation);
    }
}
*/